#include <iostream>
#include <cstdio>

#include <algorithm>
#include <cmath>

#include <vector>
#include <stack>
#include <queue>
#include <set>
#include <unordered_set>

#include <unordered_map>
#include <map>
#include <deque>
using namespace std;

int main() {
    freopen("day1.in", "r", stdin);
    freopen("day1.out", "w", stdout);
    
    std::vector<int> a;
    std::vector<int> b;
    std::string line;

    while () {
        std::string 

    }
    return 0;
}